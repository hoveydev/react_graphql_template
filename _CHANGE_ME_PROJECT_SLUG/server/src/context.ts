// TODO: import correct API classname
import { _CHANGE_ME_FIRST_API } from "./datasources/api";

export type DataSourceContext = {
  dataSources: {
    // TODO: be sure to run `npm generate` in order to get the correct type here after changing
    _CHANGE_ME_FIRST_API_DATASOURCE: _CHANGE_ME_FIRST_API;
  };
};
